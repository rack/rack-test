Ability to set headers which persist during the session
Support for multipart requests
HTTP Basic auth support
Need more specs for the many things param_string can handle
Create an error class for all Rack::Test exceptions